INSTRUCCIONES DE DESPLIEGUE Y PRUEBAS - SAFESEND PLATFORM
=========================================================
Generado: 26 de Septiembre, 2025
Plataforma: lunara.io (SafeSend)
Propósito: Guía completa para configurar entorno de desarrollo y pruebas
SISTEMAS: Windows, Linux y macOS

═══════════════════════════════════════════════════════════════════════════════

TABLA DE CONTENIDOS:
==================
1. REQUISITOS PREVIOS DEL SISTEMA
2. CONFIGURACIÓN DEL ENTORNO PYTHON
3. CLONACIÓN Y CONFIGURACIÓN DEL PROYECTO
4. CONFIGURACIÓN DE LA BASE DE DATOS
5. INSTALACIÓN DE DEPENDENCIAS
6. CONFIGURACIÓN DE VARIABLES DE ENTORNO
7. MIGRACIONES DE BASE DE DATOS
8. CREACIÓN DE USUARIO ADMINISTRADOR
9. EJECUCIÓN DEL SERVIDOR DE DESARROLLO
10. PRUEBAS DE FUNCIONALIDAD
11. PRUEBAS DE LA API
12. ACCESO AL PANEL DE ADMINISTRACIÓN DJANGO
13. SOLUCIÓN DE PROBLEMAS COMUNES
14. COMANDOS ÚTILES DE DJANGO

═══════════════════════════════════════════════════════════════════════════════

1. REQUISITOS PREVIOS DEL SISTEMA
=================================

ANTES DE COMENZAR, ASEGÚRATE DE TENER INSTALADO:
-----------------------------------------------

📋 REQUISITOS OBLIGATORIOS:
- Python 3.8 o superior (recomendado: Python 3.10)
- pip (gestor de paquetes de Python)
- Git (para clonar el repositorio)
- Navegador web moderno (Chrome, Firefox, Safari, Edge)

📋 VERIFICACIÓN DE INSTALACIONES:

🐧 LINUX / 🍎 macOS:
# Verificar Python
python3 --version
# Salida esperada: Python 3.8.x o superior

# Verificar pip
pip3 --version
# Salida esperada: pip 21.x.x o superior

# Verificar Git
git --version
# Salida esperada: git version 2.x.x

🪟 WINDOWS:
# Verificar Python (PowerShell o Command Prompt)
python --version
# Salida esperada: Python 3.8.x o superior

# Verificar pip
pip --version
# Salida esperada: pip 21.x.x o superior

# Verificar Git
git --version
# Salida esperada: git version 2.x.x

📋 SISTEMAS OPERATIVOS SOPORTADOS:
✅ Linux (Ubuntu, Debian, CentOS, etc.)
✅ macOS (10.14 o superior)
✅ Windows 10/11 (PowerShell, Command Prompt, o WSL)

🪟 INSTALACIÓN EN WINDOWS (SI NO TIENES PYTHON):
1. Descargar Python desde: https://www.python.org/downloads/
2. Durante la instalación, MARCAR "Add Python to PATH"
3. Instalar Git desde: https://git-scm.com/download/win
4. Reiniciar el sistema después de la instalación

═══════════════════════════════════════════════════════════════════════════════

2. CONFIGURACIÓN DEL ENTORNO PYTHON
===================================

CREACIÓN DE ENTORNO VIRTUAL:
---------------------------

🐧 LINUX / 🍎 macOS:
# Paso 1: Navegar al directorio del proyecto
cd /home/untitled/Documents/lunara.io

# Paso 2: Crear entorno virtual Python
python3 -m venv backend/venv

# Paso 3: Activar el entorno virtual
source backend/venv/bin/activate

# Paso 4: Verificar activación del entorno
which python
# Debe mostrar la ruta del entorno virtual

🪟 WINDOWS:
# Paso 1: Navegar al directorio del proyecto (PowerShell/Command Prompt)
cd C:\Users\TuUsuario\Documents\lunara.io
# O donde hayas clonado el proyecto

# Paso 2: Crear entorno virtual Python
python -m venv backend\venv

# Paso 3: Activar el entorno virtual

## OPCIÓN A: PowerShell (Recomendado)
backend\venv\Scripts\Activate.ps1

## OPCIÓN B: Command Prompt
backend\venv\Scripts\activate.bat

## OPCIÓN C: Git Bash (si usas Git Bash)
source backend/venv/Scripts/activate

# Paso 4: Verificar activación del entorno (Windows)
where python
# Debe mostrar la ruta del entorno virtual

🚨 PROBLEMA COMÚN EN WINDOWS - POLÍTICA DE EJECUCIÓN:
Si recibes error "execution policy" en PowerShell:
# Ejecutar como Administrador:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
# Luego reintentar la activación

🔍 VERIFICACIÓN EXITOSA:
- Terminal muestra (venv) antes del prompt
- `which python` (Linux/macOS) o `where python` (Windows) apunta al directorio venv
- `python --version` muestra la versión correcta

═══════════════════════════════════════════════════════════════════════════════

3. CLONACIÓN Y CONFIGURACIÓN DEL PROYECTO
=========================================

SI EL PROYECTO NO ESTÁ CLONADO AÚN:
----------------------------------

🐧 LINUX / 🍎 macOS:
# Paso 1: Clonar el repositorio (reemplaza con tu URL real)
git clone https://github.com/tu-usuario/lunara.io.git
cd lunara.io

# Paso 2: Verificar estructura del proyecto
ls -la
# Debes ver directorios: backend/, frontend/, y archivos .txt

🪟 WINDOWS:
# Paso 1: Clonar el repositorio (PowerShell/Command Prompt)
git clone https://github.com/tu-usuario/lunara.io.git
cd lunara.io

# Paso 2: Verificar estructura del proyecto
dir
# O para vista detallada:
dir /a
# Debes ver directorios: backend/, frontend/, y archivos .txt

ESTRUCTURA ESPERADA DEL PROYECTO:
--------------------------------
lunara.io/
├── backend/                    # Aplicación Django
│   ├── apps/                   # Aplicaciones Django
│   │   ├── accounts/          # Gestión de usuarios
│   │   └── projects/          # Gestión de proyectos
│   ├── safesend/              # Configuración principal
│   │   ├── settings/          # Configuraciones por entorno
│   │   ├── urls.py            # URLs principales
│   │   └── wsgi.py            # Configuración WSGI
│   ├── manage.py              # Script de gestión Django
│   ├── requirements.txt       # Dependencias Python
│   └── venv/                  # Entorno virtual (se crea)
├── frontend/                  # Aplicación frontend
│   ├── css/                   # Hojas de estilo
│   ├── js/                    # JavaScript
│   ├── images/                # Imágenes
│   ├── index.html             # Página principal
│   ├── signin.html            # Página de inicio de sesión
│   └── dashboard.html         # Panel de usuario
└── Project-Documentation.txt  # Documentación completa

═══════════════════════════════════════════════════════════════════════════════

4. CONFIGURACIÓN DE LA BASE DE DATOS
====================================

OPCIÓN A: USAR POSTGRESQL (RECOMENDADO - PRODUCCIÓN)
---------------------------------------------------

🎯 ESTA ES LA CONFIGURACIÓN DE PRODUCCIÓN ACTUAL

El proyecto está configurado para usar una base de datos PostgreSQL de Neon.
Los detalles de conexión están en el archivo .env que debes crear.

📋 INFORMACIÓN DE LA BASE DE DATOS ACTUAL:
- Proveedor: Neon PostgreSQL
- Región: Azure East US 2
- Conexión: SSL encriptada
- Pool de conexiones: Habilitado

OPCIÓN B: USAR SQLITE (DESARROLLO RÁPIDO)
----------------------------------------

🔧 Para pruebas rápidas sin configurar PostgreSQL:

El proyecto tiene un sistema de fallback automático a SQLite si PostgreSQL
no está disponible. Simplemente omite la configuración de PostgreSQL
y el sistema usará SQLite automáticamente.

═══════════════════════════════════════════════════════════════════════════════

5. INSTALACIÓN DE DEPENDENCIAS
==============================

INSTALACIÓN DE PAQUETES PYTHON:
------------------------------

🐧 LINUX / 🍎 macOS:
# Paso 1: Asegúrate de que el entorno virtual esté activado
# (debe aparecer (venv) en tu terminal)

# Paso 2: Actualizar pip a la última versión
pip install --upgrade pip

# Paso 3: Instalar todas las dependencias del proyecto
cd backend
pip install -r requirements.txt

# Paso 4: Verificar instalación
pip list

🪟 WINDOWS:
# Paso 1: Asegúrate de que el entorno virtual esté activado
# (debe aparecer (venv) en tu terminal)

# Paso 2: Actualizar pip a la última versión
python -m pip install --upgrade pip

# Paso 3: Instalar todas las dependencias del proyecto
cd backend
pip install -r requirements.txt

# ALTERNATIVA si hay problemas con compilación:
pip install -r requirements.txt --only-binary=all

# Paso 4: Verificar instalación
pip list

📋 DEPENDENCIAS PRINCIPALES QUE SE INSTALARÁN:
- Django==4.2.7 (framework web)
- djangorestframework==3.14.0 (API REST)
- psycopg2-binary==2.9.9 (PostgreSQL)
- djangorestframework-simplejwt==5.3.0 (autenticación JWT)
- django-cors-headers==4.3.1 (CORS)
- gunicorn==21.2.0 (servidor de producción)
- python-dotenv==1.0.0 (variables de entorno)
- Y muchas más...

🔍 VERIFICACIÓN DE INSTALACIÓN EXITOSA:

🐧 LINUX / 🍎 macOS:
```bash
python -c "import django; print(django.get_version())"
# Debe mostrar: 4.2.7

python -c "import rest_framework; print('DRF instalado correctamente')"
# Debe mostrar: DRF instalado correctamente
```

🪟 WINDOWS:
```cmd
python -c "import django; print(django.get_version())"
# Debe mostrar: 4.2.7

python -c "import rest_framework; print('DRF instalado correctamente')"
# Debe mostrar: DRF instalado correctamente
```

═══════════════════════════════════════════════════════════════════════════════

6. CONFIGURACIÓN DE VARIABLES DE ENTORNO
========================================

CREACIÓN DEL ARCHIVO .env:
-------------------------

🐧 LINUX / 🍎 macOS:
# Paso 1: Crear archivo de variables de entorno
cd backend
cp .env.example .env

# Paso 2: Editar el archivo .env con tus datos
nano .env
# O usa tu editor preferido: vim, code, gedit, etc.

🪟 WINDOWS:
# Paso 1: Crear archivo de variables de entorno (PowerShell)
cd backend
copy .env.example .env

# ALTERNATIVA en Command Prompt:
copy .env.example .env

# Paso 2: Editar el archivo .env con tus datos
notepad .env
# O usa tu editor preferido: code .env, vim .env, etc.

📝 CONTENIDO DEL ARCHIVO .env (COPIA Y PEGA):
```
# ===== CONFIGURACIÓN DE DESARROLLO =====
DEBUG=True
SECRET_KEY=tu-clave-secreta-muy-larga-y-segura-aqui-123456789

# ===== BASE DE DATOS POSTGRESQL =====
# Para usar PostgreSQL, descomenta y configura estas líneas:
# DATABASE_URL=postgresql://usuario:contraseña@host:puerto/nombre_bd
# DB_NAME=safesend_dev
# DB_USER=tu_usuario
# DB_PASSWORD=tu_contraseña
# DB_HOST=localhost
# DB_PORT=5432

# ===== BASE DE DATOS NEON (PRODUCCIÓN) =====
# Si tienes acceso a la base de datos de producción:
# DATABASE_URL=postgresql://neondb_owner:contraseña@ep-cold-night-a8z0ndqj-pooler.eastus2.azure.neon.tech/neondb?sslmode=require

# ===== CONFIGURACIÓN DE EMAIL (OPCIONAL) =====
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=tu_email@gmail.com
EMAIL_HOST_PASSWORD=tu_contraseña_app

# ===== CORS (DESARROLLO) =====
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
```

🔑 GENERAR SECRET_KEY SEGURA:

🐧 LINUX / 🍎 macOS:
```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# Copia la salida y pégala en SECRET_KEY en tu archivo .env
```

🪟 WINDOWS:
```cmd
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# Copia la salida y pégala en SECRET_KEY en tu archivo .env
```

⚠️ IMPORTANTE:
- NUNCA subas el archivo .env a Git
- El archivo .env.example es seguro para compartir
- Usa contraseñas seguras en producción

═══════════════════════════════════════════════════════════════════════════════

7. MIGRACIONES DE BASE DE DATOS
===============================

APLICACIÓN DE MIGRACIONES:
-------------------------

# Paso 1: Verificar migraciones disponibles
cd backend
python manage.py showmigrations

# Paso 2: Aplicar migraciones iniciales de Django
python manage.py migrate

# Paso 3: Verificar que las migraciones se aplicaron correctamente
python manage.py showmigrations
# Todas las migraciones deben aparecer con [X] (aplicadas)

📋 TABLAS QUE SE CREARÁN:

TABLAS DEL SISTEMA DJANGO:
- auth_user (usuarios del sistema)
- auth_group (grupos de usuarios)
- auth_permission (permisos)
- django_migrations (historial de migraciones)
- django_content_type (tipos de contenido)
- django_session (sesiones)

TABLAS DE SAFESEND:
- accounts_profile (perfiles de usuario extendidos)
- accounts_emailverification (verificación de emails)
- projects_project (proyectos principales)
- projects_milestone (hitos de proyectos)
- projects_projectinvitation (invitaciones a proyectos)
- payments_escrowaccount (cuentas de garantía)
- payments_transaction (transacciones)
- payments_paymentmethod (métodos de pago)
- payments_dispute (disputas)

🔍 VERIFICACIÓN DE ÉXITO:
```bash
python manage.py dbshell
# Si usas SQLite:
.tables
# Si usas PostgreSQL:
\dt
# Debes ver todas las tablas listadas arriba
```

═══════════════════════════════════════════════════════════════════════════════

8. CREACIÓN DE USUARIO ADMINISTRADOR
====================================

CREAR SUPERUSUARIO DJANGO:
-------------------------

# Paso 1: Crear superusuario interactivo
python manage.py createsuperuser

# Te pedirá:
# Username: admin (o el que prefieras)
# Email: admin@safesend.com (o tu email)
# Password: (elige una contraseña segura)
# Password (again): (confirma la contraseña)

ALTERNATIVAMENTE - CREAR DESDE SCRIPT:
-------------------------------------

# Crear superusuario sin interacción (útil para scripts)
python manage.py shell -c "
from django.contrib.auth import get_user_model;
User = get_user_model();
if not User.objects.filter(username='admin').exists():
    User.objects.create_superuser('admin', 'admin@safesend.com', 'admin123!')
    print('Superusuario creado exitosamente')
else:
    print('El superusuario ya existe')
"

📋 CREDENCIALES DE ACCESO RECOMENDADAS:
- Usuario: admin
- Email: admin@safesend.com
- Contraseña: admin123! (CÁMBIALA EN PRODUCCIÓN)

🔍 VERIFICACIÓN:

🐧 LINUX / 🍎 macOS:
```bash
python manage.py shell -c "
from django.contrib.auth import get_user_model;
User = get_user_model();
print(f'Total de usuarios: {User.objects.count()}');
print(f'Superusuarios: {User.objects.filter(is_superuser=True).count()}')
"
```

🪟 WINDOWS:
```cmd
python manage.py shell -c "from django.contrib.auth import get_user_model; User = get_user_model(); print(f'Total de usuarios: {User.objects.count()}'); print(f'Superusuarios: {User.objects.filter(is_superuser=True).count()}')"
```

═══════════════════════════════════════════════════════════════════════════════

9. EJECUCIÓN DEL SERVIDOR DE DESARROLLO
=======================================

INICIAR EL SERVIDOR DJANGO:
---------------------------

🐧 LINUX / 🍎 macOS:
# Paso 1: Asegúrate de estar en el directorio backend
cd backend

# Paso 2: Iniciar servidor de desarrollo
python manage.py runserver 127.0.0.1:8000

# ALTERNATIVA: Usar configuración específica de desarrollo
python manage.py runserver 127.0.0.1:8000 --settings=safesend.settings.development

🪟 WINDOWS:
# Paso 1: Asegúrate de estar en el directorio backend
cd backend

# Paso 2: Iniciar servidor de desarrollo
python manage.py runserver 127.0.0.1:8000

# ALTERNATIVA: Usar configuración específica de desarrollo
python manage.py runserver 127.0.0.1:8000 --settings=safesend.settings.development

# NOTA PARA WINDOWS: Si tienes problemas con 127.0.0.1, prueba:
python manage.py runserver localhost:8000

📋 SALIDA ESPERADA:
```
Watching for file changes with StatReloader
Performing system checks...

System check identified no issues (0 silenced).
September 26, 2025 - 10:30:45
Django version 4.2.7, using settings 'safesend.settings.development'
Starting development server at http://127.0.0.1:8000/
Quit the server with CONTROL-C.
```

🌐 URLS DE ACCESO:
- Frontend Principal: http://127.0.0.1:8000/
- Panel de Admin Django: http://127.0.0.1:8000/admin/
- API Base: http://127.0.0.1:8000/api/
- Documentación API: http://127.0.0.1:8000/api/docs/ (si está configurada)

⚠️ NOTAS IMPORTANTES:
- El servidor se recarga automáticamente cuando cambias código
- Usa Ctrl+C para detener el servidor
- El servidor solo acepta conexiones locales por seguridad
- Los archivos estáticos se sirven automáticamente en desarrollo

═══════════════════════════════════════════════════════════════════════════════

10. PRUEBAS DE FUNCIONALIDAD
============================

PRUEBA 1: ACCESO A LA PÁGINA PRINCIPAL
--------------------------------------

# Con el servidor ejecutándose, abre tu navegador y ve a:
http://127.0.0.1:8000/

✅ QUÉ DEBES VER:
- Página de inicio de SafeSend con animaciones de partículas
- Navegación responsiva con menú hamburguesa en móvil
- Botones "Comenzar Ahora" y "Iniciar Sesión" funcionales
- Diseño moderno y profesional sin errores de carga

❌ SI NO FUNCIONA:
- Verifica que el servidor esté ejecutándose
- Revisa la consola del navegador (F12) para errores
- Verifica archivos CSS/JS en la consola del servidor

PRUEBA 2: REGISTRO DE NUEVO USUARIO
----------------------------------

# Paso 1: Ir a la página de registro
http://127.0.0.1:8000/signup.html

# Paso 2: Llenar el formulario con datos de prueba:
- Nombre: Juan Pérez
- Email: juan@ejemplo.com
- Contraseña: micontraseña123!
- Confirmar contraseña: micontraseña123!
- Tipo de usuario: Freelancer (o Cliente)

# Paso 3: Hacer clic en "Crear Cuenta"

✅ COMPORTAMIENTO ESPERADO:
- Redirección automática al dashboard
- Mensaje de bienvenida
- Dashboard con estadísticas en tiempo real
- JWT tokens guardados en localStorage

🔍 VERIFICACIÓN EN BASE DE DATOS:

🐧 LINUX / 🍎 macOS:
```bash
python manage.py shell -c "
from django.contrib.auth import get_user_model;
User = get_user_model();
user = User.objects.get(email='juan@ejemplo.com');
print(f'Usuario creado: {user.username}');
print(f'Perfil: {user.profile.user_type}');
print(f'Fecha: {user.date_joined}')
"
```

🪟 WINDOWS:
```cmd
python manage.py shell -c "from django.contrib.auth import get_user_model; User = get_user_model(); user = User.objects.get(email='juan@ejemplo.com'); print(f'Usuario creado: {user.username}'); print(f'Perfil: {user.profile.user_type}'); print(f'Fecha: {user.date_joined}')"
```

PRUEBA 3: INICIO DE SESIÓN
--------------------------

# Paso 1: Ir a la página de inicio de sesión
http://127.0.0.1:8000/signin.html

# Paso 2: Usar credenciales:
- Email: juan@ejemplo.com
- Contraseña: micontraseña123!

# Paso 3: Hacer clic en "Iniciar Sesión"

✅ COMPORTAMIENTO ESPERADO:
- Autenticación exitosa
- Redirección al dashboard personalizado
- Datos del usuario cargados en tiempo real
- Navegación entre páginas funcional

PRUEBA 4: PANEL DE USUARIO (DASHBOARD)
--------------------------------------

# Con sesión iniciada, el dashboard debe mostrar:

📊 ESTADÍSTICAS EN TIEMPO REAL:
- Proyectos Activos: 2
- Proyectos Completados: 1
- Ganancias del Mes: $2,450
- Tareas Pendientes: 3
- Auto-actualización cada 30 segundos

🎛️ FUNCIONALIDADES INTERACTIVAS:
- Navegación entre secciones
- Modales de creación de proyectos
- Gestión de perfil
- Sistema de notificaciones toast

═══════════════════════════════════════════════════════════════════════════════

11. PRUEBAS DE LA API
====================

HERRAMIENTAS PARA PROBAR LA API:
-------------------------------

📋 OPCIONES DISPONIBLES:
- curl (línea de comandos) - ✅ Disponible en todos los sistemas
- Postman (aplicación gráfica) - ✅ Windows/Linux/macOS
- Insomnia (aplicación gráfica) - ✅ Windows/Linux/macOS
- HTTPie (línea de comandos amigable) - ✅ Windows/Linux/macOS
- Browser DevTools (consola del navegador) - ✅ Todos los navegadores

🪟 INSTALACIÓN DE CURL EN WINDOWS (si no está disponible):
- Windows 10/11: curl ya viene instalado
- Versiones anteriores: descargar desde https://curl.se/windows/

PRUEBA 1: ENDPOINTS PÚBLICOS
----------------------------

# Verificar que la API responde
curl http://127.0.0.1:8000/api/

# Verificar disponibilidad de email
curl "http://127.0.0.1:8000/api/auth/check-email/?email=nuevo@ejemplo.com"

# Respuesta esperada:
# {"available": true}

PRUEBA 2: REGISTRO VIA API
-------------------------

# Registro de usuario vía API
curl -X POST http://127.0.0.1:8000/api/auth/register/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "usuario_api",
    "email": "api@ejemplo.com",
    "password": "contraseña123!",
    "first_name": "Usuario",
    "last_name": "API",
    "user_type": "freelancer"
  }'

✅ RESPUESTA ESPERADA:
```json
{
  "access": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "refresh": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "user": {
    "id": 2,
    "username": "usuario_api",
    "email": "api@ejemplo.com",
    "first_name": "Usuario",
    "last_name": "API"
  }
}
```

PRUEBA 3: AUTENTICACIÓN VIA API
------------------------------

# Login vía API
curl -X POST http://127.0.0.1:8000/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "api@ejemplo.com",
    "password": "contraseña123!"
  }'

# Guardar el token de la respuesta para siguientes pruebas
TOKEN="tu_token_aqui"

PRUEBA 4: ENDPOINTS PROTEGIDOS
-----------------------------

# Obtener perfil de usuario (requiere autenticación)
curl -H "Authorization: Bearer $TOKEN" \
  http://127.0.0.1:8000/api/auth/profile/

# Obtener estadísticas del dashboard
curl -H "Authorization: Bearer $TOKEN" \
  http://127.0.0.1:8000/api/auth/dashboard/stats/

# Listar proyectos del usuario
curl -H "Authorization: Bearer $TOKEN" \
  http://127.0.0.1:8000/api/projects/my_projects/

✅ TODAS LAS RESPUESTAS DEBEN:
- Retornar código HTTP 200
- Incluir datos JSON válidos
- No mostrar errores de autenticación

═══════════════════════════════════════════════════════════════════════════════

12. ACCESO AL PANEL DE ADMINISTRACIÓN DJANGO
============================================

ACCESO AL ADMIN PANEL:
---------------------

# Paso 1: Abrir el panel de administración
http://127.0.0.1:8000/admin/

# Paso 2: Usar credenciales del superusuario
- Usuario: admin
- Contraseña: admin123! (o la que hayas configurado)

📋 SECCIONES DISPONIBLES EN EL ADMIN:

🔐 AUTENTICACIÓN Y AUTORIZACIÓN:
- Users (Usuarios del sistema)
- Groups (Grupos de usuarios)

👤 ACCOUNTS:
- Profiles (Perfiles extendidos de usuarios)
- Email verifications (Verificaciones de email)

📁 PROJECTS:
- Projects (Proyectos principales)
- Milestones (Hitos de proyectos)
- Project invitations (Invitaciones)

💰 PAYMENTS:
- Escrow accounts (Cuentas de garantía)
- Transactions (Transacciones)
- Payment methods (Métodos de pago)
- Disputes (Disputas)

OPERACIONES ADMINISTRATIVAS:
---------------------------

📊 VERIFICAR DATOS:
- Ver usuarios registrados
- Revisar proyectos creados
- Monitorear transacciones
- Gestionar disputas

🔧 GESTIÓN DE USUARIOS:
- Activar/desactivar usuarios
- Cambiar tipos de usuario
- Resetear contraseñas
- Verificar emails manualmente

📈 ANÁLISIS:
- Estadísticas de uso
- Proyectos por estado
- Usuarios activos
- Transacciones por mes

═══════════════════════════════════════════════════════════════════════════════

13. SOLUCIÓN DE PROBLEMAS COMUNES
=================================

PROBLEMA 1: ERROR AL INSTALAR DEPENDENCIAS
------------------------------------------

🪟 ERRORES ESPECÍFICOS DE WINDOWS:

❌ ERROR: "error: Microsoft Visual C++ 14.0 is required"
🔧 SOLUCIÓN:
1. Instalar Microsoft C++ Build Tools desde:
   https://visualstudio.microsoft.com/visual-cpp-build-tools/
2. O usar paquetes precompilados:
   pip install --only-binary=all psycopg2-binary
3. ALTERNATIVA RÁPIDA:
   pip install psycopg2-binary --no-deps

❌ ERROR: "error: Microsoft Visual Studio 14.0 is required"
🔧 SOLUCIÓN:
1. Instalar "Build Tools for Visual Studio 2019/2022"
2. Marcar "C++ build tools" durante la instalación
3. Reiniciar y volver a intentar

🐧 ERRORES ESPECÍFICOS DE LINUX:

❌ ERROR: "pg_config executable not found"
🔧 SOLUCIÓN:
```bash
# Ubuntu/Debian:
sudo apt-get update
sudo apt-get install postgresql-dev python3-dev libpq-dev

# CentOS/RHEL/Fedora:
sudo yum install postgresql-devel python3-devel
# O en versiones nuevas:
sudo dnf install postgresql-devel python3-devel

# Arch Linux:
sudo pacman -S postgresql-libs python
```

🍎 ERRORES ESPECÍFICOS DE macOS:

❌ ERROR: "command 'gcc' failed" o "clang: error"
🔧 SOLUCIÓN:
```bash
# Instalar Xcode command line tools:
xcode-select --install

# Si ya están instalados, actualizarlos:
sudo xcode-select --reset

# Alternativa con Homebrew:
brew install postgresql
```

PROBLEMA 2: ERRORES DE BASE DE DATOS
-----------------------------------

❌ ERROR: "django.db.utils.OperationalError: could not connect to server"
🔧 SOLUCIÓN:
1. Verificar que PostgreSQL esté ejecutándose
2. Comprobar credenciales en .env
3. Usar SQLite como fallback (comentar DATABASE_URL en .env)

❌ ERROR: "relation does not exist"
🔧 SOLUCIÓN:
```bash
# Resetear migraciones:
python manage.py migrate --run-syncdb
python manage.py migrate
```

❌ ERROR: "FATAL: password authentication failed"
🔧 SOLUCIÓN:
1. Verificar usuario y contraseña en .env
2. Comprobar permisos de la base de datos
3. Usar autenticación por trust temporalmente

PROBLEMA 3: ERRORES DEL SERVIDOR
-------------------------------

❌ ERROR: "ModuleNotFoundError: No module named 'django'"
🔧 SOLUCIÓN:

🐧 LINUX / 🍎 macOS:
```bash
# Verificar que el entorno virtual esté activado:
which python
# Debe apuntar al directorio venv

# Reactivar entorno virtual:
source backend/venv/bin/activate
```

🪟 WINDOWS:
```cmd
# Verificar que el entorno virtual esté activado:
where python
# Debe apuntar al directorio venv

# Reactivar entorno virtual (PowerShell):
backend\venv\Scripts\Activate.ps1

# Reactivar entorno virtual (Command Prompt):
backend\venv\Scripts\activate.bat
```

❌ ERROR: "Invalid HTTP_HOST header"
🔧 SOLUCIÓN:
- Agregar tu host a ALLOWED_HOSTS en settings
- O usar 127.0.0.1:8000 en lugar de localhost:8000

❌ ERROR: "CORS policy" en el navegador
🔧 SOLUCIÓN:
- Verificar CORS_ALLOWED_ORIGINS en settings
- Agregar tu dominio a la configuración CORS

PROBLEMA 4: PROBLEMAS DE FRONTEND
--------------------------------

❌ ERROR: CSS o JS no se cargan
🔧 SOLUCIÓN:

🐧 LINUX / 🍎 macOS:
```bash
# Verificar archivos estáticos:
python manage.py collectstatic --noinput

# Verificar configuración STATIC_URL en settings
```

🪟 WINDOWS:
```cmd
# Verificar archivos estáticos:
python manage.py collectstatic --noinput

# Verificar configuración STATIC_URL en settings
```

❌ ERROR: "Failed to fetch" en API calls
🔧 SOLUCIÓN:
1. Verificar que el servidor backend esté ejecutándose
2. Comprobar URLs de API en frontend/js/api.js
3. Revisar configuración CORS

❌ ERROR: 404 en rutas del frontend
🔧 SOLUCIÓN:
- Verificar que las URLs estén configuradas en safesend/urls.py
- Comprobar que los archivos HTML existan en frontend/

═══════════════════════════════════════════════════════════════════════════════

14. COMANDOS ÚTILES DE DJANGO
=============================

GESTIÓN DE BASE DE DATOS:
------------------------

# Ver estado de migraciones
python manage.py showmigrations

# Crear nuevas migraciones tras cambios en modelos
python manage.py makemigrations

# Aplicar migraciones pendientes
python manage.py migrate

# Resetear base de datos (CUIDADO: BORRA TODOS LOS DATOS)
python manage.py flush

# Crear datos de prueba
python manage.py loaddata fixtures/initial_data.json

# Hacer backup de datos
python manage.py dumpdata > backup.json

GESTIÓN DE USUARIOS:
------------------

# Crear superusuario
python manage.py createsuperuser

# Cambiar contraseña de usuario
python manage.py changepassword nombre_usuario

# Shell interactivo de Django
python manage.py shell

# Shell con configuración específica
python manage.py shell --settings=safesend.settings.development

DEPURACIÓN Y DESARROLLO:
----------------------

# Verificar configuración del proyecto
python manage.py check

# Verificar configuración de deployment
python manage.py check --deploy

# Ver configuración actual
python manage.py diffsettings

# Recopilar archivos estáticos
python manage.py collectstatic

# Limpiar sesiones expiradas
python manage.py clearsessions

TESTING:
-------

# Ejecutar todas las pruebas
python manage.py test

# Ejecutar pruebas de una app específica
python manage.py test apps.accounts

# Ejecutar pruebas con verbose output
python manage.py test --verbosity=2

# Ejecutar pruebas con coverage
coverage run manage.py test
coverage report

INFORMACIÓN DEL SISTEMA:
----------------------

# Ver versión de Django
python manage.py version

# Ver configuración de URLs
python manage.py show_urls

# Ver estadísticas de la base de datos
python manage.py dbshell -c "\dt"

# Inspeccionar modelo específico
python manage.py inspectdb tabla_especifica

═══════════════════════════════════════════════════════════════════════════════

RESUMEN DE PASOS PARA PRUEBA RÁPIDA:
====================================

🚀 SECUENCIA COMPLETA (COPY-PASTE READY):

🐧 LINUX / 🍎 macOS:
```bash
# 1. Navegar al proyecto
cd /home/untitled/Documents/lunara.io

# 2. Activar entorno virtual
source backend/venv/bin/activate

# 3. Instalar dependencias (si no están instaladas)
cd backend
pip install -r requirements.txt

# 4. Aplicar migraciones
python manage.py migrate

# 5. Crear superusuario (opcional, si no existe)
python manage.py createsuperuser

# 6. Iniciar servidor
python manage.py runserver 127.0.0.1:8000
```

🪟 WINDOWS (PowerShell):
```powershell
# 1. Navegar al proyecto
cd C:\Users\TuUsuario\Documents\lunara.io

# 2. Activar entorno virtual
backend\venv\Scripts\Activate.ps1

# 3. Instalar dependencias (si no están instaladas)
cd backend
pip install -r requirements.txt

# 4. Aplicar migraciones
python manage.py migrate

# 5. Crear superusuario (opcional, si no existe)
python manage.py createsuperuser

# 6. Iniciar servidor
python manage.py runserver 127.0.0.1:8000
```

🪟 WINDOWS (Command Prompt):
```cmd
# 1. Navegar al proyecto
cd C:\Users\TuUsuario\Documents\lunara.io

# 2. Activar entorno virtual
backend\venv\Scripts\activate.bat

# 3. Instalar dependencias (si no están instaladas)
cd backend
pip install -r requirements.txt

# 4. Aplicar migraciones
python manage.py migrate

# 5. Crear superusuario (opcional, si no existe)
python manage.py createsuperuser

# 6. Iniciar servidor
python manage.py runserver 127.0.0.1:8000
```

🌐 URLS PARA PROBAR:
- Frontend: http://127.0.0.1:8000/
- Admin: http://127.0.0.1:8000/admin/
- API: http://127.0.0.1:8000/api/
- Dashboard: http://127.0.0.1:8000/dashboard.html

✅ VERIFICACIÓN DE ÉXITO:
- Página principal carga sin errores
- Registro de usuario funciona
- Login redirecciona al dashboard
- Dashboard muestra datos en tiempo real
- Panel de admin es accesible
- API responde a peticiones

📞 SOPORTE:
===========
Si encuentras problemas que no están cubiertos en esta guía:
1. Revisa los logs del servidor Django
2. Verifica la consola del navegador (F12)
3. Comprueba la configuración del archivo .env
4. Consulta la documentación oficial de Django

Estado del documento: Completo y actualizado
Generado: 26 de Septiembre, 2025
Versión: 2.0 (Español - Multiplataforma)
Sistemas soportados: Windows, Linux, macOS

═══════════════════════════════════════════════════════════════════════════════