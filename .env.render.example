# === Render Backend Environment Variables ===
# Set these in the Render dashboard for pbp-api and pbp-ingestion services

# Database (auto-set by Render from pbp-db, but override with asyncpg driver)
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname

# Redis (auto-set by Render from pbp-redis)
REDIS_URL=redis://red-xxx:6379

# Kafka (Upstash or Confluent Cloud)
KAFKA_BOOTSTRAP_SERVERS=xxx.upstash.io:9092

# ESPN polling (ingestion worker only)
ESPN_POLL_INTERVAL_SECONDS=30
