/**
 * Pricing.jsx - EXAMPLE React component for pricing section
 *
 * This is a COMPLETE EXAMPLE showing how to migrate the Pricing section.
 * To activate:
 * 1. Rename this file to "Pricing.jsx" (remove .example)
 * 2. Add script tag to index.html
 * 3. Update react-app.js to include Pricing component
 * 4. Remove static pricing HTML from index.html (lines 146-176)
 */

const Pricing = () => {
  const pricingPlans = [
    {
      id: 'freelancers',
      icon: '💎',
      title: 'For Freelancers',
      price: 'Free',
      subtitle: 'Protect your work, maximize earnings',
      description: 'Get paid faster with escrow protection and automated milestone management.',
      ctaText: '✨ Start Hustling',
      featured: false
    },
    {
      id: 'clients',
      icon: '🏢',
      title: 'For Clients',
      price: 'Free',
      subtitle: 'Protect your money, hire with confidence',
      description: 'Only pay when milestones are approved. Transparent, secure, stress-free transactions.',
      ctaText: '💡 Hire Smart',
      featured: true
    }
  ];

  const handleSignUp = (e, planType) => {
    e.preventDefault();

    // You could pass plan type to signup page via URL params
    if (window.LunaraNavigate) {
      window.LunaraNavigate(`signup.html?plan=${planType}`);
    } else {
      // Fallback
      window.location.href = `signup.html?plan=${planType}`;
    }
  };

  return (
    <section id="pricing" className="section fade-in">
      <div className="container">
        <h2 className="section-title">Simple, Transparent Pricing</h2>
        <p className="section-subtitle">
          Only pay when milestones are met. No hidden fees. Full transparency, always.
        </p>
        <div className="pricing-grid">
          {pricingPlans.map((plan) => (
            <div
              key={plan.id}
              className={`pricing-card ${plan.featured ? 'featured' : ''}`}
            >
              <div className="pricing-icon">{plan.icon}</div>
              <h3>{plan.title}</h3>
              <div className="pricing-cost">
                <span className="cost-main">{plan.price}</span>
                <span className="cost-sub">{plan.subtitle}</span>
              </div>
              <p>{plan.description}</p>
              <a
                href="#"
                className="btn btn-primary"
                onClick={(e) => handleSignUp(e, plan.id)}
              >
                {plan.ctaText}
              </a>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
};

// Export for use in react-app.js
window.Pricing = Pricing;

/**
 * ACTIVATION INSTRUCTIONS:
 *
 * 1. Rename this file:
 *    mv Pricing.jsx.example Pricing.jsx
 *
 * 2. Add to index.html (in the React components section):
 *    <script type="text/babel" src="/react-components/Pricing.jsx"></script>
 *
 * 3. Update react-app.js - Modify waitForComponents():
 *    if (window.Hero && window.Features && window.Pricing) {
 *      clearInterval(checkInterval);
 *      resolve();
 *    }
 *
 * 4. Update react-app.js - Modify renderApp():
 *    const { Hero, Features, Pricing } = window;
 *
 *    const App = () => {
 *      return React.createElement(
 *        React.Fragment,
 *        null,
 *        React.createElement(Hero),
 *        React.createElement(Features),
 *        React.createElement(Pricing)  // Add this line
 *      );
 *    };
 *
 * 5. Remove static pricing HTML from index.html:
 *    Delete lines 146-176 (the <section id="pricing">...</section>)
 *
 * 6. Test in browser:
 *    - Refresh page
 *    - Check pricing section appears correctly
 *    - Test "Start Hustling" and "Hire Smart" buttons
 *    - Verify styling matches original
 *
 * ADVANCED: Making Pricing Interactive
 *
 * You can enhance this component with React state:
 *
 * const Pricing = () => {
 *   const [billingPeriod, setBillingPeriod] = React.useState('monthly');
 *   const [selectedPlan, setSelectedPlan] = React.useState(null);
 *
 *   // Add toggle for monthly/annual pricing
 *   // Add hover effects
 *   // Add comparison tooltips
 *   // Add real-time price calculator
 * }
 */